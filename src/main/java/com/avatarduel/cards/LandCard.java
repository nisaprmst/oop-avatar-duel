package com.avatarduel.cards;

public class LandCard extends Card {
    public LandCard(int id, String name, String description, Element element) {
        super(id, name, description, element);
    }
}
